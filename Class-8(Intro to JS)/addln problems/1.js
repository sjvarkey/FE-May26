
// Function to log each element of an array multiplied by 2
function main(arr) {
	for (let i = 0; i < arr.length; i++) {
		console.log(arr[i] * 2);
	}
}

// Example usage:
main([1, 2, 3]); // You can change the array to test other inputs